#Diario

# Síntesis

> **Nota reconstruida el 04-ago** desde `git log` de `DESARROLLO` (sin Daily). Cubre el **domingo
> 02-ago** (solo equipo — sin commits míos) y el **lunes 03-ago**. Anterior: [[2026-08-01]].

## Lunes 03-ago — mi trabajo (3 commits, 11:46–15:19)

Continúa el carril documental del sábado y cierra un pendiente del expediente:

- `6c7e7343` **Mejora expediente de pedidos** — el expediente muestra creación, ingesta y
  aprobación, usando datos de respaldo cuando no están disponibles para mantener un historial
  consistente (`PedidosController` + `PedidoExpedienteViewModel` + vista `Expediente.cshtml`).
- `2ad598c7` **Refactoriza Mis Documentos** — interfaz renovada: vista previa segura, descargas
  integradas, mejor consulta y revisión, alineada a la versión actual de Axon (+605 líneas;
  actualiza los contract tests de seguridad de descarga y documentos de transportista).
- `d3a62e20` **Detalles Mis Documentos** — propietario con miniaturas/iconos y diseño responsivo,
  fechas unificadas con `fmt(...)`, lógica de previsualización/descarga simplificada y fix de
  comparación de tipos al cerrar filas del panel (−41 líneas netas).

**El carril documental queda así:** acreditación API + panel + visor (sábado, [[2026-08-01]]) →
expediente de pedidos y Mis Documentos renovados (lunes).

## Domingo 02-ago — el equipo (sin commits míos)

- **Chris:** `2aa70266` **check de responsabilidad al ingreso a CEDIS** — registra quién palomea
  cada punto de control (migración `20260803003223_AddTripControlPointCheckedByName`, rehace
  Manifiestos/MonitorPatio); `591f9c5e` + `8f6b4453` **PODs** — seeder de plantillas de
  requisitos, panel POD en Tracking, tipos de evidencia; `08c1fbf4` tickets de cita (pase de
  acceso PDF); `d3af92e3` refactor de Planificación — el commit se llama *"order mikl refactor"*.
- **Bruno:** packing (límite de cajas por operación y por pedido, deep-link `/Packing`, picker de
  productos, flujo Confirmado→Packing), avatar de operadores, fotos de vehículo en el pase de
  acceso PDF.
- **Richi:** P8 — fallback de HU selladas + prioriza pedidos sin HU, sube el clamp máximo.

## Lunes — el equipo

- **Bruno:** ruta lechera en la UI de Armado (botón **"Armar viaje lechera"**, detección y
  highlight), fix del modal "Nueva cita", loaders de botones (`axon-btn-loader.js`), polling y
  límite de fotos en PE; `cb811268` "Detalles y liberación" (bump de `ApiMetaData`/
  `WebAppMetaData` — huele a release).

## Estado / riesgos

- ⚠️ **7 migraciones sin liberar a QA**: 3 del 30-jul + `AddEvidenceGapsCaceWaves`,
  `AddConsolidationPolicyAndCanConsolidate` (31-jul), `AddDocumentAccreditation` (01-ago),
  `AddTripControlPointCheckedByName` (02-ago). Coordinar deploy de API antes de que QA pruebe
  cualquiera de estos flujos.
- ⚠️ `d3af92e3` (*"order mikl refactor"*, Chris) reescribió `PlanificacionController` y
  `planificacion.js` — **revisar que no pise mi trabajo del carril de pedidos** antes de seguir
  ahí.
- `DESARROLLO` local == `origin`; sin commits del martes 04 al momento de reconstruir.

## Pendientes que siguen vivos (de [[2026-07-31]])

1. T-03 regla de igualdad por tipo de entrega (el checkbox existe; la regla no).
2. Programa de embarques: quitar mapa y botón "sacar" — zona compartida con Bruno, ahora sobre su
   rediseño del fin de semana. Coordinar antes de tocar.
3. Reconfirmar con Chris las 4 dudas abiertas: umbral alto valor (>150k), valor de factura
   calculado vs capturado, "vista 192", logos bitmap-en-BD vs carpeta compartida.
4. Smoke test de vistas tras los merges masivos — sigue sin constar como hecho.
