#Diario

# Síntesis

> **Nota reconstruida el 04-ago** desde `git log` de `DESARROLLO` (sin Daily). Cubre el **sábado
> 01-ago** y el cierre del **viernes 31 por la tarde** — la nota [[2026-07-31]] se reconstruyó a
> las 10:46 y no alcanzó a registrar los 7 commits míos de esa tarde. Contexto previo:
> [[2026-07-31]] · tareas abiertas: [[Tareas-2026-07-29]].

## Viernes 31 (tarde) — carril de pedidos, 7 commits míos (13:55–17:39)

Aterrizó casi todo el **pendiente 1** de [[2026-07-31]] (destino operativo visible, aviso de
destino documental faltante, separación origen/destino):

- `4688a1e9` **Mejora cálculo de mercancía** — prorrateo del total declarado, totales derivados
  y flag de estimación (`OrderService` + `OrderItemDto`, con tests `OrderServiceIdentityTests`).
- `71a6ed3f` **Avance de empaque en el listado** — indicadores de piezas/cajas por pedido, sin
  abrir el detalle.
- `6f6bd463` + `f07b71bc` **Listado de pedidos ampliado y refactorizado** — facturación, valor de
  mercancía, clasificación de entrega y cadena comercial con tienda destino; reorganización de la
  expansión y legibilidad.
- `b4d84f07` **Filtro de destinos** — prioriza el punto de entrega operativo cuando difiere del
  destino documental.
- `49c00d63` **Edición de pedidos** — reorganiza captura origen/destino, advierte si falta
  destino documental y simplifica el flujo de validación (probable retiro del sweetalert —
  **verificar en la vista**).
- `2987f050` **Validación de pedidos** — la entrega planificada no puede ser anterior a la
  solicitud; muestra el motivo del bloqueo y asiste la captura de pedidos urgentes.

## Sábado 01-ago — carril nuevo: **acreditación documental** (5 commits, 11:12–16:29)

Día completo en el módulo de documentos, de la API al portal:

- `6d5b93c5` **Acreditación documental (API)** — proceso de acreditación **independiente de la
  vigencia**: validar/rechazar documentos con registro de quién y cuándo revisó. Entidad
  `Document` ampliada, migración **`20260801164845_AddDocumentAccreditation`**,
  `DocumentValidationStatusEnum`, DTOs de listado/validación y permisos nuevos en
  `AxonPermissions`. Plan y aclaración en `docs/operacion/85_ACREDITACION_FLOTA_ACLARACION_Y_PLAN.md`.
- `b331e85f` **Fix permisos** — registra en `PermissionSeeder` los permisos de gestión documental
  faltantes (sin esto el panel no era accesible).
- `61f1ee3a` **Panel de acreditación (WebMvc)** — revisar, acreditar o rechazar desde el portal,
  con estado e historial de validación por documento.
- `e51f5160` **Visor de documentos** — imágenes inline cuando el contenido es válido;
  `SafeDocumentDownloadResult` fuerza descarga ante archivos inconsistentes (+ tests).
- `e4733127` **Cierre del carril** — acreditación cableada al portal de transportistas
  (conductores, perfil, expediente) y filtros por transportista para la revisión documental.

## Contexto del equipo (viernes tarde-noche / sábado)

- **Chris:** `870c0c5e` cierra huecos de evidencia CACE — liberación documental, recolección
  externa, papelería — con migración `20260731210147_AddEvidenceGapsCaceWaves`; `564fbbac`
  `CanConsolidate` + compose de manifiestos del PE (migración
  `20260731142732_AddConsolidationPolicyAndCanConsolidate`); `6414b993` **ruta lechera** en VMP
  (RutaLechera controller, mapa `route-map.js`).
- **Bruno:** rediseño Armado de Rutas → **Planificación** como workbench (mapa fuera, tabs,
  matriz porteador×unidad al cotizar), base de citas en el detalle del PE, fallback pricing V1
  por zona LINK; sábado: límite de cajas por operación en packing y limpieza de ~183k líneas de
  `.trx` commiteadas por accidente.
- **Richi:** P8 — paginación del adapter, cache de cajas, respeto de `OrdenSurtido`, soporte tipo
  ALL; bump para release QA (`3c610756`).

## Estado / riesgos

- ⚠️ **Migraciones acumuladas sin liberar a QA**: a las 3 del 30-jul se suman
  `AddEvidenceGapsCaceWaves`, `AddConsolidationPolicyAndCanConsolidate` (31-jul, Chris) y
  `AddDocumentAccreditation` (01-ago, mía). La BD QA queda cada vez más atrás.
- ⚠️ La zona ProgramaEmbarques/Planificación cambió fuerte con el rediseño de Bruno — el
  pendiente "quitar mapa y botón sacar" de [[2026-07-31]] hay que re-evaluarlo sobre el código
  nuevo, no sobre el viejo.
- `DESARROLLO` local == `origin`, working tree limpio.
